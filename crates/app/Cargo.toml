[package]
name = "platform_manager_app"
version = "0.1.0"
edition = "2021"

[dependencies]
platform_manager_core = { path = "../core" }
thiserror = { workspace = true }
tracing = { workspace = true }
serde_json = { workspace = true }

